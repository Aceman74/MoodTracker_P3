<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_very_happy_frame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/banana_yellow"
    tools:context=".controller.VeryHappyActivity">

    <ImageButton
        android:id="@+id/activity_very_happy_smiley_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:src="@drawable/smiley_super_happy"
        android:contentDescription="@string/Actual_mood" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom">

        <ImageButton
            android:id="@+id/activity_very_happy_note_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_note_add_black"
            android:layout_alignParentStart="true"
            android:padding="20dp"
            android:contentDescription="@string/Add_Note" />

        <ImageButton
            android:id="@+id/activity_very_happy_history_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:background="@android:color/transparent"
            android:contentDescription="@string/mood_history"
            android:src="@drawable/ic_history_black"
            android:layout_alignParentEnd="true"
            android:padding="20dp"/>

    </RelativeLayout>

</FrameLayout>